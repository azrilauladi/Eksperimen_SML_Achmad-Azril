name: Exam Score Preprocessing Pipeline

on:
  push:
    branches: [ main, master ]
    paths:
      - 'Exam_Score_Prediction_raw/**'
      - 'preprocessing/**'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Manual trigger

jobs:
  preprocess:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy scikit-learn
        
    - name: Run preprocessing
      run: |
        cd preprocessing
        python automate_Achmad-Azril.py --input ../Exam_Score_Prediction_raw/Exam_Score_Prediction.csv --output ./
        
    - name: List preprocessed files
      run: |
        echo "=== Preprocessed Files ==="
        ls -la preprocessing/
        echo ""
        echo "=== Preview ExamScore_preprocessing.csv ==="
        head -5 preprocessing/ExamScore_preprocessing.csv
        
    - name: Upload preprocessed data as artifact
      uses: actions/upload-artifact@v4
      with:
        name: preprocessed-data
        path: |
          preprocessing/ExamScore_train.csv
          preprocessing/ExamScore_test.csv
          preprocessing/ExamScore_preprocessing.csv
          preprocessing/scaler.pkl
        retention-days: 30
        
    - name: Commit preprocessed data
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add preprocessing/ExamScore_train.csv preprocessing/ExamScore_test.csv preprocessing/ExamScore_preprocessing.csv preprocessing/scaler.pkl || true
        git diff --staged --quiet || git commit -m "Auto-update: Preprocessed data [skip ci]"
        git push || echo "No changes to push"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}